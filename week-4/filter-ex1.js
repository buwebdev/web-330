/*
============================================
; Title:  filter-ex1.js
; Author: Professor Krasso
; Date:   26 January 2023
; Description: Filter example
;===========================================
*/

const numbers = [5, 6, 2, 10, 15] // Array of numbers.

// Return a new array of numbers that are greater than or equal to 10.
const result = numbers.filter(num => num >= 10)

// Display the results in a terminal window. 
console.log(result)